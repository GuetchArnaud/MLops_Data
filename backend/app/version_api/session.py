from fastapi import FastAPI
from typing import any 
from datetime import timedelta

router = FastAPI(
    title= "MLops_Data"
)

""" 
    We can use this template to build the session of our API !!
"""
